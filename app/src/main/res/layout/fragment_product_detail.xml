<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@android:drawable/ic_menu_revert"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:tint="@color/green_super"/>

        <ImageView
            android:id="@+id/ivDetailImage"
            android:layout_width="match_parent"
            android:layout_height="250dp"
            android:src="@android:drawable/ic_menu_gallery"
            android:layout_marginTop="16dp"/>

        <!-- Categoria Evidenziata -->
        <com.google.android.material.chip.Chip
            android:id="@+id/chipCategory"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="CATEGORIA"
            app:chipBackgroundColor="@color/green_light"
            android:textColor="@color/green_super"
            android:textStyle="bold"
            android:layout_marginTop="16dp"/>

        <TextView
            android:id="@+id/tvDetailName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Nome Prodotto"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginTop="8dp"/>

        <TextView
            android:id="@+id/tvDetailDesc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Descrizione completa del prodotto..."
            android:layout_marginTop="8dp"
            android:textSize="16sp"
            android:textColor="#666666"/>

        <View android:layout_width="match_parent" android:layout_height="1dp" android:background="#EEEEEE" android:layout_marginVertical="24dp"/>

        <!-- Zona Prezzi -->
        <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content" android:orientation="horizontal" android:gravity="bottom">

            <TextView
                android:id="@+id/tvFinalPrice"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="€ 0.00"
                android:textSize="32sp"
                android:textStyle="bold"
                android:textColor="@color/green_super"/>

            <TextView
                android:id="@+id/tvOldPrice"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="€ 0.00"
                android:textSize="18sp"
                android:layout_marginStart="12dp"
                android:textColor="#999999"
                android:visibility="gone"/> <!-- Visibile solo se c'è lo sconto -->
        </LinearLayout>

        <TextView
            android:id="@+id/tvSavings"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Risparmi € 0.50 (15%)"
            android:textColor="@color/orange_accent"
            android:textStyle="bold"
            android:visibility="gone"
            android:layout_marginTop="4dp"/>

        <!-- Pulsanti Azione -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnFavorite"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:text="AGGIUNGI AI PREFERITI"
            android:layout_marginTop="32dp"
            app:icon="@android:drawable/btn_star"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnDetailAdd"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="AGGIUNGI AL CARRELLO"
            android:layout_marginTop="12dp"
            android:backgroundTint="@color/green_super"/>

    </LinearLayout>
</ScrollView>